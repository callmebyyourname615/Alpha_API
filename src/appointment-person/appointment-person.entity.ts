// The active entity for `appointment_persons` lives beside Appointment so the
// relation and all API routes share one schema definition. This module remains
// as an empty compatibility file for old import paths, but must not register a
// second @Entity for the same table.
export {};
